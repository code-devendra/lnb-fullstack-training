/* Create a guest list for a summer party and verify coming people whether they are allowed
or not. */

let guestList = ["Gulab", "Himanshu", "Gulnaj", "Suraj", "Arjun"];
let name = prompt("Enter your name before entering into party : ");
if (arr.includes(name)) {
    console.log(name, " is invited");
} else {
    console.log(name, " is not invited");
}